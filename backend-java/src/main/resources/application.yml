server:
  port: ${PORT:8080}

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:prod}
  jackson:
    property-naming-strategy: SNAKE_CASE

jwt:
  secret: ${JWT_SECRET:EL_PATRON_HN_JWT_SECRET_2026_DEV_MUST_CHANGE_IN_PROD_32CHARS!}
  expiration-ms: 86400000

app:
  cookie:
    secure: false

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

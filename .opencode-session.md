## Active
- DB reset: users (4) + service_types (8) kept, transactional data cleared (schema.sql drops + data.sql reinitializes on every restart due to `sql.init.mode: always`).
- Products (30) and clients (180) must be reseeded via API scripts if needed.
- **`invoice_items` table implemented**: new table with `venta_id` (FK → sales CASCADE), `tipo_item` (PRODUCTO/SERVICIO), nullable `producto_id`/`servicio_id` via mutually exclusive CHECK constraint, plus `descripcion`, `cantidad`, `precio_unitario`, `descuento`, `isv`, `subtotal`, `total_linea`.
- **Backend**: `InvoiceItem` entity, `InvoiceItemRepository`, `InvoiceItemRequest`/`InvoiceItemResponse` DTOs, `InvoiceItemService` (auto-calculates subtotal/total_linea from cantidad×precioUnitario−descuento+ISV%), `InvoiceItemController` (CRUD + `GET /api/invoice-items/by-sale/{ventaId}` + `DELETE /api/invoice-items/by-sale/{ventaId}`).
- **Frontend**: `InvoiceItem` interface in `types.ts`, `api.ts` methods (`getInvoiceItems`, `createInvoiceItem`, `updateInvoiceItem`, `deleteInvoiceItem`, `deleteInvoiceItemsBySale`).
- **SalesView** changed from looping `onAddSale` per-item (creating N sales) to sending ALL items in one `onAddSale` call → `App.tsx:handleAddNewSale` creates 1 sale + N invoice_items via `api.createInvoiceItem()`.
- Job modal now tracks `serviceId` to link invoice_items to service_types.

### Blocked
- Products/clients need reseeding (no `data.sql` entries for them; previously seeded via PowerShell).

## Next Move
- Re-seed products and clients via seed scripts if needed for testing.
- Implement invoice_items display in SalesView (show items when viewing a sale).
- Add edit/delete inline controls for invoice_items.
